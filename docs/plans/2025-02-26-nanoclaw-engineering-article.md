# NanoClaw：Skills协议驱动的AI Agent架构革新

## 文档写作规划

> **目标读者**: 混合受众（后端/架构师、全栈工程师、技术管理者）
> **风格**: 务实代码分析型
> **核心主题**: Skills协议与扩展模式（重点）、AI驱动架构迁移
> **发布渠道**: 内部技术分享

---

## 文章结构规划

### 第一部分：引言（约600字）

**目标**: 快速建立上下文，引出Skills协议的核心价值

**内容要点**:
1. 背景：AI Agent开发的困境
   - 传统：功能越多，代码越膨胀，信任越难建立
   - OpenClaw：50万行 vs NanoClaw：500行

2. NanoClaw的核心创新不只是"小"
   - 而是如何用"Skills协议"重新定义功能扩展方式
   - 从"代码库膨胀"到"按需自修改"

3. 本文结构
   - 简要介绍核心技术架构（容器隔离、消息循环）
   - 重点深入：Skills协议的设计、实现与工程意义
   - 实践建议：如何在你的系统中应用类似思想

---

### 第二部分：核心架构简析（约1200字）

**目标**: 简要介绍NanoClaw的三大核心机制，为后续Skills协议讨论做铺垫

#### 2.1 容器隔离：物理级的安全边界

**核心设计**（简要概述，不展开代码细节）：
- 每次Agent调用都创建新的Linux容器
- 显式挂载原则：只有明确挂载的目录才能访问
- Main group只读项目代码，其他group只能访问自己的数据
- 操作系统级别的权限墙，不是应用层的安全假设

**关键代码位置**（供参考）：
- `src/container-runner.ts:57-200` - buildVolumeMounts
- `src/container-runner.ts:104-163` - 群组数据边界

#### 2.2 异步消息循环：解耦与可靠

**核心设计**（简要概述）：
```
WhatsApp -> SQLite(持久化) -> Polling Loop -> Container(Claude SDK) -> Response
```

**关键设计决策**:
1. **立即advance cursor**：消息一旦读取就更新lastTimestamp，防止崩溃重启后重复处理
2. **Cursor分离**：`lastTimestamp`（已读消息游标）与`lastAgentTimestamp`（已处理消息游标）分离
3. **Crash Recovery**：`recoverPendingMessages`在启动时检查未处理消息

**关键代码位置**（供参考）：
- `src/index.ts:328-423` - startMessageLoop
- `src/db.ts` - SQLite持久化层

#### 2.3 Issue #30的教训：maxTurns与防御性编程

**简要案例**：
- 问题：用户要求设置芬兰语提醒，结果32秒内收到21条重复确认消息
- 原因：早期代码未设置`maxTurns`，Agent陷入无限循环
- 修复：`maxTurns: 10`硬性边界
- 教训：永远不要假设AI会自己知道何时停止

---

### 第三部分：Skills协议——从PR到自修改代码（约3500字）【重点章节】

**目标**: 这是本文的核心章节。深入剖析Skills协议的设计理念、实现机制、工程价值与行业意义。

#### 3.1 开源协作模式的范式转移

**3.1.1 传统模式的困境**

深入分析问题：
- **代码库膨胀**：每个新功能都永久增加代码体积
  - 支持Telegram = adapter代码 + 依赖声明 + 条件路由 + 配置项
  - 支持Slack = 再一套adapter代码 + 依赖声明 + 条件路由 + 配置项
  - 10个平台 = 10套代码，即使每个用户只用1-2个平台

- **配置蔓延（Configuration Sprawl）**：
  ```yaml
  # 传统模式的配置示例
  channels:
    whatsapp:
      enabled: true
      api_key: ${WHATSAPP_API_KEY}
      webhook_url: ${WEBHOOK_URL}
    telegram:
      enabled: false  # 用户不需要，但配置项仍在
      api_key: ${TELEGRAM_API_KEY}
    slack:
      enabled: false
      api_key: ${SLACK_API_KEY}
    # ... 10+ more platforms
  ```

- **理解难度指数增长**：
  - 新贡献者需要理解整个抽象层
  - 调试时需要追踪跨越多个文件的调用链
  - "为什么这个bug只在Telegram出现？"

- **测试矩阵爆炸**：
  - N个平台 × M个功能 × K个配置 = 不可维护的测试组合

**3.1.2 NanoClaw的激进选择**

核心宣言：
> "Don't add features. Add skills."

这不是营销话术，而是根本性的架构哲学转变：

| 维度 | 传统模式 | Skills模式 |
|------|---------|-----------|
| **功能扩展方式** | 修改主代码库 | 执行Skill生成新代码 |
| **代码所有权** | 项目维护者 | 每个用户自己的fork |
| **定制化程度** | 通用框架中妥协 | 完全为个人需求定制 |
| **代码体积** | 随功能增加而膨胀 | 按需生成，无冗余代码 |
| **配置复杂度** | 配置文件爆炸 | 零配置，代码即配置 |
| **审计难度** | 需要理解整个抽象层 | 只关注生成的具体代码 |

#### 3.2 Skills协议详解

**3.2.1 核心概念：什么是Skill？**

Skill不是：
- ❌ 可执行的二进制插件
- ❌ 运行时加载的动态链接库
- ❌ 配置文件或脚本

Skill是：
- ✅ 用自然语言编写的专家指令
- ✅ 指导Claude Code进行源代码转换的"菜谱"
- ✅ 声明式的代码生成规范

**类比理解**：
```
传统插件 = 预编译的零件，安装时插入到预留的插槽中
Skill = 食谱，告诉厨师（Claude）如何用现有食材（当前代码）
        一步一步做出新菜（新功能）
```

**3.2.2 Skill文件结构**

Skill文件位置：`.claude/skills/{skill-name}/SKILL.md`

完整结构解析：

```markdown
---
# 元数据层（Metadata Layer）- YAML Frontmatter
skill_id: add-telegram              # 唯一标识符
name: Add Telegram Support          # 显示名称
version: 1.0.0                      # 版本
author: @qwibitai                    # 作者
description: |                      # 简短描述（关键！）
  Adds Telegram as a communication channel alongside WhatsApp.

  Use when: 用户想要用Telegram替代或补充WhatsApp
  Prerequisites: 需要有Telegram Bot Token

tags: [channel, telegram, messaging]  # 标签，用于搜索
created_at: 2025-01-15
updated_at: 2025-02-20
---

# 完整指令层（Full Instructions）- Markdown主体

## Overview

本Skill将指导你通过以下步骤为NanoClaw添加Telegram支持：
1. 添加Telegraf SDK依赖
2. 创建Telegram频道实现
3. 更新主入口以初始化Telegram
4. 测试验证

## Prerequisites Check

在开始之前，请确认：
- [ ] 用户已提供Telegram Bot Token（从@BotFather获取）
- [ ] 当前代码可以正常运行（`npm run dev`无错误）

## Step 1: Add Dependency

### Action
编辑`package.json`，在`dependencies`中添加`telegraf`：

```json
{
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^1.0.0",
    "baileys": "^6.7.0",
    "telegraf": "^4.16.0"  // <-- 添加这一行
  }
}
```

### Verification
运行`npm install`确保依赖安装成功，无错误。

### Rollback
如果安装失败：
1. 删除`node_modules`和`package-lock.json`
2. 恢复原`package.json`
3. 重新运行`npm install`

## Step 2: Create Telegram Channel

### Action
创建新文件`src/channels/telegram.ts`：

```typescript
/**
 * Telegram Channel Implementation for NanoClaw
 *
 * This module provides Telegram Bot integration using the Telegraf library.
 */
import { Telegraf, Context } from 'telegraf';
import { Channel, NewMessage } from '../types.js';
import { logger } from '../logger.js';

export interface TelegramChannelOptions {
  token: string;
  onMessage?: (chatJid: string, msg: NewMessage) => void;
  onChatMetadata?: (
    chatJid: string,
    timestamp: string,
    name?: string,
    channel?: string,
    isGroup?: boolean,
  ) => void;
}

export class TelegramChannel implements Channel {
  private bot: Telegraf;
  private connected = false;
  private opts: TelegramChannelOptions;

  constructor(opts: TelegramChannelOptions) {
    this.opts = opts;
    this.bot = new Telegraf(opts.token);
  }

  getName(): string {
    return 'telegram';
  }

  isConnected(): boolean {
    return this.connected;
  }

  async connect(): Promise<void> {
    // Handle text messages
    this.bot.on('text', async (ctx) => {
      const chatId = ctx.chat?.id;
      const messageId = ctx.message?.message_id;
      const from = ctx.from;

      if (!chatId || !messageId) return;

      const chatJid = `tg:${chatId}`;
      const timestamp = new Date(ctx.message.date * 1000).toISOString();

      // Create NewMessage
      const message: NewMessage = {
        id: String(messageId),
        chat_jid: chatJid,
        sender: String(from?.id || 'unknown'),
        sender_name: from?.first_name || 'Unknown',
        content: ctx.message.text || '',
        timestamp: timestamp,
        is_from_me: false,
        is_bot_message: false,
      };

      // Store chat metadata
      const chatName = ctx.chat?.type === 'private'
        ? from?.first_name
        : ctx.chat?.title;

      this.opts.onChatMetadata?.(
        chatJid,
        timestamp,
        chatName,
        'telegram',
        ctx.chat?.type !== 'private',
      );

      // Call message handler
      this.opts.onMessage?.(chatJid, message);
    });

    await this.bot.launch();
    this.connected = true;

    logger.info('Telegram bot connected');
  }

  async disconnect(): Promise<void> {
    await this.bot.stop();
    this.connected = false;
    logger.info('Telegram bot disconnected');
  }

  async sendMessage(jid: string, text: string): Promise<void> {
    const chatId = jid.replace('tg:', '');
    await this.bot.telegram.sendMessage(chatId, text);
  }

  ownsJid(jid: string): boolean {
    return jid.startsWith('tg:');
  }

  async setTyping(jid: string, typing: boolean): Promise<void> {
    // Telegram doesn't have a persistent typing indicator
    // We could implement a temporary one if needed
  }
}
```

### Key Points

1. **JID Format**: Telegram使用`tg:${chatId}`格式
2. **Message Handling**: 转换Telegraf的Context到NanoClaw的NewMessage
3. **Metadata**: 提取chat名称和类型（private/group）
4. **Error Handling**: 需要添加适当的错误处理和日志

### Verification Steps

1. 编译检查：`npm run build`应该无错误
2. 类型检查：`npx tsc --noEmit`应该通过
3. 代码风格：`npm run lint`应该通过

## Step 3: Update Main Entry Point

### Action

编辑`src/index.ts`，添加Telegram channel初始化：

```typescript
// ... 现有imports ...
import { TelegramChannel } from './channels/telegram.js';

// ... 在main()函数中，WhatsApp初始化之后 ...

// Telegram Channel (optional)
if (process.env.TELEGRAM_BOT_TOKEN) {
  logger.info('Initializing Telegram channel...');
  const telegram = new TelegramChannel({
    token: process.env.TELEGRAM_BOT_TOKEN,
    onMessage: channelOpts.onMessage,
    onChatMetadata: channelOpts.onChatMetadata,
  });
  channels.push(telegram);
  await telegram.connect();
}
```

### Environment Variable

确保用户设置环境变量：

```bash
export TELEGRAM_BOT_TOKEN="your-bot-token-from-botfather"
```

### Verification

1. 启动NanoClaw：`npm run dev`
2. 检查日志，确认"Telegram bot connected"
3. 无错误退出码

## Step 4: Testing

### Test Cases

1. **基础消息测试**
   - 向Telegram Bot发送消息
   - 检查NanoClaw日志，确认收到消息
   - 确认消息内容正确

2. **响应测试**
   - 发送包含trigger的消息（如"@Andy hello"）
   - 确认Agent响应
   - 检查响应内容是否合理

3. **群组测试**
   - 将Bot添加到Telegram群组
   - 发送消息
   - 确认能正确识别群组消息

### Debugging

如果测试失败：

1. **Bot无响应**
   - 检查token是否正确
   - 检查是否已启动Bot（/start）
   - 检查日志中的连接错误

2. **消息未到达**
   - 检查`onMessage`回调是否被调用
   - 添加日志在TelegramChannel的消息处理函数中

3. **Agent不响应**
   - 确认trigger pattern匹配
   - 检查Agent日志，确认prompt是否正确

## Rollback Plan

如果添加Telegram后出现严重问题：

1. 注释掉`src/index.ts`中的Telegram初始化代码
2. 删除`src/channels/telegram.ts`
3. 从`package.json`中移除`telegraf`依赖
4. 运行`npm install`清理依赖
5. 测试确认系统恢复正常

## Advanced Topics (Optional)

### Webhook Mode

生产环境建议使用Webhook而不是Polling：

```typescript
// 在TelegramChannel中添加
async setupWebhook(url: string): Promise<void> {
  await this.bot.telegram.setWebhook(url);
}
```

### Media Handling

处理图片、文件等非文本消息：

```typescript
this.bot.on('photo', async (ctx) => {
  // 下载图片
  // 调用Vision API
  // 将结果作为消息内容
});
```

## Conclusion

完成本Skill后，你的NanoClaw将同时支持WhatsApp和Telegram。用户可以通过任一渠道与Agent交互，体验完全一致。

如果遇到问题，参考"Troubleshooting"和"Debugging"章节。如果需要移除Telegram支持，按照"Rollback Plan"操作。
```

#### 3.2.3 Skills协议的工作原理

**执行流程详解**:

```
用户输入: /add-telegram

系统处理:
1. 扫描 .claude/skills/ 目录，找到匹配的技能
2. 读取 SKILL.md 的元数据层（YAML Frontmatter）
3. 确认技能与用户意图匹配
4. 加载完整指令层（Markdown主体）到上下文
5. 调用 Claude Code 执行技能指令
6. 逐条执行 Implementation Steps
7. 每个步骤都有 Verification，失败则执行 Rollback
8. 完成后，代码库已包含Telegram支持
```

**关键机制**: Progressive Disclosure（渐进式暴露）

```
三层模型：

1. 元数据层 (Metadata Layer)
   - 位置：YAML Frontmatter
   - 内容：skill_id, name, description, tags
   - Token占用：约50-100 tokens
   - 加载时机：系统启动时
   - 数量上限：可同时挂载数千个skills

2. 完整指令层 (Full Instructions)
   - 位置：Markdown主体
   - 内容：详细的操作步骤、代码示例、验证方法
   - Token占用：约2000-5000 tokens
   - 加载时机：Skill被触发时
   - 数量上限：受限于上下文窗口

3. 关联资源层 (Linked Files)
   - 位置：与SKILL.md同目录的辅助文件
   - 内容：预设脚本、测试用例、模板文件
   - Token占用：按需加载
   - 加载时机：指令中显式引用时

优势：
- 空闲时只占用极少的Token（元数据层）
- 需要时才加载大量Token（完整指令层）
- 避免上下文被不相关的技能信息挤占
```

#### 3.2.4 Skills协议的优势深度分析

**对比维度详解**:

| 维度 | 传统PR模式 | Skills模式 | 深层含义 |
|------|-----------|-----------|---------|
| **代码所有权** | 社区共享，维护者合并 | 每个用户拥有自己的代码 | 从"共享代码"到"共享知识" |
| **定制化能力** | 在通用框架中妥协 | 完全为个人需求定制 | 从"配置"到"改造" |
| **学习曲线** | 需要理解整个抽象层 | 只需理解生成的具体代码 | 从"理解框架"到"理解实现" |
| **迁移成本** | 升级可能破坏定制 | 每次执行skill都是新鲜生成 | 从"维护"到"重建" |
| **社区协作** | 通过PR贡献代码 | 通过Skill贡献知识 | 从"代码共享"到"知识共享" |

**工程价值详解**:

1. **消除死代码（Dead Code Elimination）**

   传统模式的问题：
   ```typescript
   // 即使不用Telegram，代码也在那里
   import { TelegramAdapter } from './adapters/telegram';

   if (config.channels.telegram.enabled) {
     app.use('/telegram', new TelegramAdapter());
   }
   // 这行代码永远存在，永远占用空间，永远可能成为bug来源
   ```

   Skills模式的解决：
   ```typescript
   // 执行skill前，没有Telegram相关代码
   // 执行skill后，只有需要的代码
   import { TelegramChannel } from './channels/telegram';
   // 代码是按需生成的，没有多余的部分
   ```

2. **定制化即代码修改（Customization = Code Changes）**

   传统模式的问题：
   ```yaml
   # 想要修改行为？在配置文件中妥协
   behavior:
     response_style: "concise"  # 只能是预设选项之一
     max_length: 500          # 数字配置
     enable_emojis: false     # 布尔开关
     # 想要的行为不在选项中？没办法
   ```

   Skills模式的解决：
   ```typescript
   // 直接修改代码，做任何想要的定制
   // 比如：改变响应格式
   function formatResponse(text: string): string {
     // 完全自定义的格式化逻辑
     // 不是配置，是代码
     return `🤖 **Assistant Response**\n\n${text}\n\n---\n_This response was generated just for you_`;
   }
   ```

3. **渐进式暴露（Progressive Disclosure）与Token效率**

   技术细节：
   ```
   假设上下文窗口：100K tokens

   传统模式：
   - 加载10个适配器的Schema：10 × 500 tokens = 5K tokens
   - 加载所有配置选项：3K tokens
   - 加载帮助文档：2K tokens
   - 总计：10K tokens（10%的上下文被固定占用）

   Skills模式：
   - 空闲时：只加载元数据 10 × 50 tokens = 500 tokens（0.5%）
   - 需要时：只加载当前skill的指令 2K tokens
   - 总计：2.5K tokens（2.5%，且是按需）

   效果：
   - 上下文效率提升4倍
   - 可以挂载更多skills而不影响性能
   - 响应更快，因为无关信息不会加载
   ```

#### 3.2.5 Skills协议的行业意义

**对开源社区的启示**：

1. **从代码共享到知识共享**
   - 传统：贡献者提交代码，维护者审核合并
   - Skills：贡献者分享知识（如何修改代码），用户自己执行
   - 优势：知识比代码更持久，不会因为版本更新而过时

2. **从集中式到分布式**
   - 传统：主仓库是唯一的"真值"
   - Skills：每个用户的fork都是"真值"，根据他们的需求定制
   - 优势：没有需要维护的"通用"版本，只有特定的"专用"版本

3. **从兼容性到可变性**
   - 传统：升级需要考虑向后兼容性
   - Skills：每次执行都是新鲜生成，不需要兼容旧版本
   - 优势：可以自由地重构和重新设计，不用担心破坏旧功能

**对软件工程的启示**：

1. **代码即配置（Code as Configuration）**
   - 传统：用配置文件控制行为
   - Skills：用代码生成控制行为
   - 优势：代码比配置更灵活，可以表达任意复杂的逻辑

2. **生成式编程（Generative Programming）**
   - 传统：程序员直接编写最终代码
   - Skills：程序员编写生成代码的指令
   - 优势：指令比代码更简洁，可以生成大量复杂代码

3. **自修改系统（Self-Modifying Systems）**
   - 传统：系统功能是静态的，由程序员预先定义
   - Skills：系统功能是动态的，可以根据需要自我修改
   - 优势：系统可以适应不断变化的需求，而不需要人工干预

---

### 第四部分：实践建议（约800字）

**目标**: 提炼可复用的工程实践建议

#### 4.1 何时考虑类似Skills的架构

**适用场景**：
- 功能高度可定制化，每个用户需求差异大
- 代码库膨胀导致维护困难
- 需要快速响应多样化需求
- 团队有能力维护多个定制化版本

**不适用场景**：
- 功能标准化，用户定制需求少
- 团队规模小，无法维护多个版本
- 需要严格的版本控制和兼容性保证

#### 4.2 渐进式采用策略

**阶段1：实验（1-2个skill）**
- 选择1-2个最常用的定制需求
- 编写对应的skill文件
- 在小范围内测试

**阶段2：验证（5-10个skill）**
- 扩展skill库
- 建立skill编写规范
- 收集用户反馈

**阶段3：全面采用（所有定制通过skill）**
- 所有新功能通过skill实现
- 建立skill审核机制
- 形成生态系统

#### 4.3 关键成功因素

1. **清晰的元数据**
   - skill的描述要准确、完整
   - 明确使用场景和先决条件
   - 提供清晰的预期效果

2. **详尽的指令**
   - 步骤要具体、可执行
   - 提供验证方法
   - 包含回滚方案

3. **持续的测试**
   - 定期执行skill验证
   - 建立自动化测试
   - 跟踪版本兼容性

---

### 第五部分：结语（约400字）

**内容要点**：

1. **核心洞察**
   - NanoClaw的革命性不在于"小"，而在于Skills协议重新定义了功能扩展方式
   - 从"共享代码"到"共享知识"，从"通用框架"到"专用定制"

2. **行业意义**
   - Skills协议可能是开源协作的新范式
   - 按需生成、无冗余、自修改的架构可能是对抗软件膨胀的新思路

3. **行动建议**
   - 不要只看NanoClaw的具体实现，要看背后的设计思想
   - 思考：你的系统是否也有"每个用户都想要不同功能"的问题？
   - 是否可以尝试用类似Skills的方式解决？

4. **最后的思考**
   - 当AI能够理解和修改代码，软件开发会变成什么样？
   - Skills协议可能只是一个开始，未来或许所有软件都是"自修改"的
   - 问题是：你准备好迎接这个未来了吗？

---

## 调整后的预计字数

| 部分 | 预计字数 | 调整说明 |
|------|---------|---------|
| 第一部分：引言 | 600 | 保持简洁，快速进入主题 |
| 第二部分：核心架构简析 | 1200 | 合并原2、3部分，简要介绍 |
| 第三部分：Skills协议详解 | 3500 | **重点章节**，大幅扩展 |
| 第四部分：实践建议 | 800 | 合并原5部分，精简建议 |
| 第五部分：结语 | 400 | 简洁收尾，留下思考 |
| **总计** | **约7500字** | Skills协议占比47%，符合要求 |

---

## 关键检查项

### 技术准确性
- [ ] Skill文件结构与实际NanoClaw skill一致
- [ ] `add-telegram` skill示例可直接执行
- [ ] Progressive Disclosure解释准确

### 内容深度
- [ ] Skills协议不仅解释"是什么"，还要解释"为什么"
- [ ] 行业意义部分有足够洞察，不只是表面描述
- [ ] 实践建议可操作，不只是理论

### 风格一致性
- [ ] 保持务实代码分析型风格
- [ ] 即使是理论部分也有具体例子支撑
- [ ] 避免空洞的形容词，用具体数据说话

---

## 下一步行动

1. **确认调整**：确认新结构符合"2、3、5合并简要，4大幅扩展"的要求
2. **代码验证**：验证`add-telegram` skill示例的代码准确性
3. **撰写执行**：按新结构开始撰写，重点投入第三部分的写作
4. **内部审查**：发布前进行技术准确性和敏感信息检查

---

*规划文档版本: 2.0（已调整结构）*
*调整日期: 2025-02-26*
*适用仓库: qwibitai/nanoclaw*
